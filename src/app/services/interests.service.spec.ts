import { TestBed } from '@angular/core/testing';
import {InterestingTopicsService} from './interests.service'

describe('InterestingTopicsService', () => {
  
  beforeEach(() => {
    
    TestBed.configureTestingModule({
      
    });
    
  });

  it('should init', () => {
      
  });
    
  
  describe('getInterestingTopicsNearMe()', () => {
    
    it('should return an FirebaseListObservable', () => {
      
    });
    
  });
});